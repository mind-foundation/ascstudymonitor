mutation MoveUpInQueue($channel: String!, $publication: ID!) {
  moveUpInQueue(channel: $channel, publication: $publication) {
    success
    message
    queue(channel: $channel) {
      id
      slug
      title
    }
  }
}
