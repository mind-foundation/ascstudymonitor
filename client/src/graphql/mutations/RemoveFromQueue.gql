mutation RemoveFromQueue($channel: String!, $publication: ID!) {
  removeFromQueue(channel: $channel, publication: $publication) {
    success
    message
    queue(channel: $channel) {
      id
      slug
      title
    }
  }
}
