mutation MoveDownInQueue($channel: String!, $publication: ID!) {
  moveDownInQueue(channel: $channel, publication: $publication) {
    success
    message
    queue(channel: $channel) {
      id
      slug
      title
    }
  }
}
